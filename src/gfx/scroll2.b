def main() {
    setVideoMode(2);
    fillRect(0, 0, 160, 200, COLOR_RED);
    setBackground(COLOR_YELLOW);
    scroll(2, 0);
    fillRect(-6, 40, 2, 160, COLOR_GREEN);
    while(1 = 1) {
        updateVideo();
    }
}
